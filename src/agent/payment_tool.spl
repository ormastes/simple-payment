use std.io_runtime

struct PaymentToolRequest:
    val action: text
    val token: text
    val amount_cents: int
    val currency: text

struct PaymentToolResponse:
    val success: bool
    val message: text
    val transaction_id: text

fn handle_payment_tool(req: PaymentToolRequest) -> PaymentToolResponse:
    # TODO(M6): dispatch payment action via SFM authz gate
    # SECURITY: req.token must be an opaque vault token, never raw card data
    # SECURITY: money movement actions require PaymentAuthz capability
    return PaymentToolResponse {
        success: false,
        message: "TODO: not implemented",
        transaction_id: ""
    }

fn validate_agent_request(req: PaymentToolRequest) -> bool:
    # TODO(M6): ensure request contains only opaque tokens, no raw PAN/CVV
    # reject if token looks like a card number (starts with digit, 13-19 chars)
    return false

fn list_payment_methods_for_agent() -> list<text>:
    # TODO(M6): return stored payment method summaries (last4 + brand only)
    return []
