use std.io_runtime

fn cli_add_card() -> text:
    # TODO(M2): prompt for card details, tokenize via vault, return token ID
    # SECURITY: card data must not be logged or persisted in raw form
    return ""

fn cli_list_cards() -> list<text>:
    # TODO(M2): list stored payment methods (masked: ****1234 Visa)
    return []

fn cli_charge(token_id: text, amount_cents: int, currency: text) -> text:
    # TODO(M2): charge via gateway using vault token
    return ""

fn cli_refund(transaction_id: text, amount_cents: int) -> text:
    # TODO(M2): refund a previous charge
    return ""

fn cli_payment_main(args: list<text>) -> int:
    # TODO(M2): parse CLI args, dispatch to add/list/charge/refund
    # subcommands: add-card, list-cards, charge, refund, status
    println("simple-payment CLI — not yet implemented")
    return 1
