use std.io_runtime

fn luhn_check(card_number: text) -> bool:
    # TODO(M1): implement Luhn algorithm
    # double every second digit from right, sum digits, check mod 10 == 0
    return false

fn detect_brand(card_number: text) -> CardBrand:
    # TODO(M1): detect card brand from BIN (first 6 digits)
    # Visa: starts with 4
    # Mastercard: starts with 51-55 or 2221-2720
    # Amex: starts with 34 or 37
    # Discover: starts with 6011, 622126-622925, 644-649, 65
    # JCB: starts with 3528-3589
    # UnionPay: starts with 62
    return CardBrand.Unknown

fn mask_card_number(card_number: text) -> text:
    # TODO(M1): mask all but last 4 digits -> "****-****-****-1234"
    return ""

fn validate_expiry(month: int, year: int) -> bool:
    # TODO(M1): check expiry is in the future
    return false

fn extract_last4(card_number: text) -> text:
    # TODO(M1): return last 4 characters of card number
    return ""
